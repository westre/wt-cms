import { provideRouter, RouterConfig } from '@angular/router';
import { FilesComponent } from './files.component';

export const FilesRoutes: RouterConfig = [
    { path: 'files', component: FilesComponent }
];